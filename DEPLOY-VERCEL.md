# 🚀 Деплой на Vercel (рекомендуется)

Vercel работает с приватными репозиториями и проще в настройке, чем GitHub Pages.

## Быстрый деплой

### Вариант 1: Через веб-интерфейс Vercel

1. **Перейдите на [vercel.com](https://vercel.com)**
2. **Войдите через GitHub**
3. **Нажмите "Add New Project"**
4. **Выберите ваш репозиторий** `almaty-dorm-finder`
5. **Настройки:**
   - Framework Preset: **Vite**
   - Root Directory: `./` (по умолчанию)
   - Build Command: `npm run build` (по умолчанию)
   - Output Directory: `dist` (по умолчанию)
6. **Нажмите "Deploy"**

Готово! 🎉 Ваше приложение будет доступно через несколько минут.

### Вариант 2: Через Vercel CLI

```bash
# Установите Vercel CLI
npm i -g vercel

# Задеплойте
vercel

# Для продакшена
vercel --prod
```

## Преимущества Vercel

✅ **Работает с приватными репозиториями**  
✅ **Автоматический деплой** при каждом push  
✅ **Бесплатный SSL сертификат**  
✅ **CDN по всему миру**  
✅ **Автоматические preview для PR**  
✅ **Простая настройка**  

## Настройка автоматического деплоя

После первого деплоя Vercel автоматически:
- Задеплоит при каждом push в `main`
- Создаст preview для каждого Pull Request
- Уведомит вас о статусе деплоя

## Environment Variables (если нужны)

Если нужно настроить переменные окружения:
1. Перейдите в Project Settings → Environment Variables
2. Добавьте нужные переменные:
   - `VITE_API_URL` (если используете внешний API)
   - `VITE_BASE_PATH` (обычно не нужен для Vercel)

## URL после деплоя

Vercel автоматически создаст URL:
- `https://almaty-dorm-finder.vercel.app` (или похожий)
- Можно настроить кастомный домен в настройках проекта

---

## Альтернатива: Netlify

Если предпочитаете Netlify:

1. Перейдите на [netlify.com](https://netlify.com)
2. Войдите через GitHub
3. Нажмите "Add new site" → "Import an existing project"
4. Выберите репозиторий
5. Настройки:
   - Build command: `npm run build`
   - Publish directory: `dist`
6. Нажмите "Deploy site"

---

## Сравнение хостингов

| Хостинг | Приватные репозитории | Бесплатно | Простота |
|---------|---------------------|-----------|----------|
| **Vercel** | ✅ Да | ✅ Да | ⭐⭐⭐⭐⭐ |
| **Netlify** | ✅ Да | ✅ Да | ⭐⭐⭐⭐ |
| **GitHub Pages** | ❌ Нет* | ✅ Да | ⭐⭐⭐ |
| **Cloudflare Pages** | ✅ Да | ✅ Да | ⭐⭐⭐⭐ |

*GitHub Pages работает с приватными репозиториями только на платном плане

**Рекомендация:** Используйте **Vercel** - самый простой и быстрый вариант! 🚀

