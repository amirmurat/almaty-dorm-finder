# 🚀 Деплой на GitHub Pages

## Быстрый старт

### 1. Подготовка репозитория

```bash
# Убедитесь, что все изменения закоммичены
git add .
git commit -m "Setup for GitHub Pages"
git push origin main
```

### 2. Включение GitHub Pages

1. Перейдите в **Settings** → **Pages** вашего репозитория
2. В разделе **Source** выберите **"GitHub Actions"**
3. Сохраните изменения

### 3. Автоматический деплой

После включения GitHub Actions, при каждом push в `main` или `master` ветку будет автоматически запускаться деплой.

**Или запустите вручную:**
- Перейдите в **Actions** → **Deploy to GitHub Pages (Manual)**
- Нажмите **"Run workflow"**

## Настройка base path

### Если репозиторий называется `username.github.io`:
- Base path: `/` (по умолчанию)
- URL: `https://username.github.io/`

### Если репозиторий называется иначе (например, `almaty-dorm-finder`):
- Base path: `/almaty-dorm-finder/`
- URL: `https://username.github.io/almaty-dorm-finder/`

**Автоматически определяется** из имени репозитория в GitHub Actions.

## Ручной деплой (альтернатива)

Если хотите задеплоить вручную:

```bash
# 1. Установите имя репозитория (замените на ваше)
export VITE_BASE_PATH=/almaty-dorm-finder/

# 2. Соберите проект
npm run build

# 3. Задеплойте через gh-pages
npm run deploy
```

Или используйте готовый скрипт:
```bash
npm run build:gh-pages
npm run deploy
```

## Важные замечания

⚠️ **Бэкенд не работает на GitHub Pages**
- GitHub Pages - это статический хостинг
- Приложение автоматически использует **localStorage** как fallback
- Все данные хранятся локально в браузере пользователя

✅ **Что работает:**
- Все функции фронтенда
- Поиск и фильтры
- Карта (Leaflet/OpenStreetMap)
- Аутентификация (localStorage)
- Заявки и платежи (localStorage)

## Проверка деплоя

1. После успешного деплоя проверьте **Actions** - должен быть зеленый статус
2. Перейдите в **Settings** → **Pages** - там будет ссылка на ваш сайт
3. Обычно деплой занимает 1-2 минуты

## Troubleshooting

### Проблема: 404 ошибки на страницах
**Решение:** Убедитесь, что base path настроен правильно в `vite.config.ts`

### Проблема: Карта не загружается
**Решение:** Проверьте, что все координаты (`geo.lat`, `geo.lng`) заполнены в `dorms.ts`

### Проблема: Стили не применяются
**Решение:** Убедитесь, что `index.html` использует правильные пути к CSS

## Дополнительные настройки

### Кастомный домен
1. В **Settings** → **Pages** добавьте ваш домен
2. Настройте DNS записи согласно инструкциям GitHub

### Environment variables
Создайте файл `.env.production`:
```
VITE_BASE_PATH=/almaty-dorm-finder/
VITE_API_URL=
```

---

**Готово!** 🎉 Ваше приложение будет доступно на GitHub Pages.

